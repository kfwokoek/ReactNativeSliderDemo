import { StyleSheet } from 'react-native'

export const ViewStyles = StyleSheet.create({
    safeView: {
        height: '100%',
        width: '100%',
        backgroundColor: '#000000',
        alignItems: 'center',
    },
})